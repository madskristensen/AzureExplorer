<platformUI:DialogWindow x:Class="AzureExplorer.ResourceGroup.Dialogs.CreateResourceGroupDialog"
                        x:ClassModifier="internal"
                        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                        xmlns:platformUI="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
                        xmlns:toolkit="clr-namespace:Community.VisualStudio.Toolkit;assembly=Community.VisualStudio.Toolkit"
                        toolkit:Themes.UseVsTheme="True"
                        Title="Create Resource Group"
                        Width="400"
                        Height="220"
                        WindowStartupLocation="CenterOwner"
                        ShowInTaskbar="False"
                        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Resource Group Name:" Margin="0,0,0,4" />
        <TextBox Grid.Row="1" x:Name="ResourceGroupNameBox" Margin="0,0,0,12" />

        <TextBlock Grid.Row="2" Text="Location:" Margin="0,0,0,4" />
        <ComboBox Grid.Row="3" x:Name="LocationComboBox" Margin="0,0,0,12" DisplayMemberPath="DisplayName" />

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Create" Width="75" Margin="0,0,8,0" IsDefault="True" Click="OnCreateClick" />
            <Button Content="Cancel" Width="75" IsCancel="True" />
        </StackPanel>
    </Grid>
</platformUI:DialogWindow>
