<platformUI:DialogWindow x:Class="AzureExplorer.KeyVault.Dialogs.CreateKeyDialog"
                        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                        xmlns:platformUI="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
                        xmlns:toolkit="clr-namespace:Community.VisualStudio.Toolkit;assembly=Community.VisualStudio.Toolkit"
                        toolkit:Themes.UseVsTheme="True"
                        Title="Add Key"
                        Width="400"
                        SizeToContent="Height"
                        WindowStartupLocation="CenterOwner"
                        ShowInTaskbar="False"
                        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Key Name -->
        <TextBlock Grid.Row="0" Text="Key Name:" Margin="0,0,0,4" />
        <TextBox Grid.Row="1" x:Name="KeyNameBox" Margin="0,0,0,12" TextChanged="OnKeyNameChanged" />

        <!-- Key Type -->
        <TextBlock Grid.Row="2" Text="Key Type:" Margin="0,0,0,4" />
        <ComboBox Grid.Row="3" x:Name="KeyTypeComboBox" Margin="0,0,0,12" SelectionChanged="OnKeyTypeChanged" />

        <!-- Key Size -->
        <TextBlock Grid.Row="4" Text="Key Size:" Margin="0,0,0,4" />
        <ComboBox Grid.Row="5" x:Name="KeySizeComboBox" Margin="0,0,0,16" />

        <!-- Buttons -->
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CreateButton" Content="Add" Width="75" Margin="0,0,8,0" IsDefault="True" IsEnabled="False" Click="OnCreateClick" />
            <Button Content="Cancel" Width="75" IsCancel="True" />
        </StackPanel>
    </Grid>
</platformUI:DialogWindow>
